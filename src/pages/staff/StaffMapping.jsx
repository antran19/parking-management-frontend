/**
 * StaffMapping — Sơ đồ bãi xe cho Staff (Tùng phụ trách)
 *
 * TODO (Tùng): Implement:
 * - Hiển thị zones theo tầng với màu sắc trạng thái
 * - WebSocket listener cập nhật real-time
 * - Xem chi tiết zone khi click
 */
export default function StaffMapping({ onLogout }) {
  return (
    <div className="min-h-screen bg-gray-900 text-white p-6">
      <h1 className="text-2xl font-bold">🗺️ Sơ đồ bãi xe</h1>
      <p className="text-gray-400 mt-2">Coming soon — Tùng phụ trách</p>
    </div>
  );
}
