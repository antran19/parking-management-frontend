/**
 * StaffHistory — Lịch sử gửi xe (Tùng phụ trách)
 *
 * TODO (Tùng): Implement:
 * - Bảng lịch sử sessions (filter theo ngày, biển số, trạng thái)
 * - GET /staff/sessions
 */
export default function StaffHistory({ onLogout }) {
  return (
    <div className="min-h-screen bg-gray-900 text-white p-6">
      <h1 className="text-2xl font-bold">📜 Lịch sử gửi xe</h1>
      <p className="text-gray-400 mt-2">Coming soon — Tùng phụ trách</p>
    </div>
  );
}
